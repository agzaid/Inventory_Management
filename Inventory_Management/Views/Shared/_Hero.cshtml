<style>
    /* #hero {
        position: relative;
        background: linear-gradient(135deg, #f8f9fa, #e9f5ee);
        min-height: 80vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: flex-start;
        padding: 80px 10%;
        text-align: left;
        overflow: hidden;
    } */

        #hero h4 {
            font-size: 18px;
            font-weight: 500;
            color: #28a745;
            margin-bottom: 10px;
            text-transform: uppercase;
            letter-spacing: 1.5px;
        }

        #hero h2 {
            font-size: 40px;
            font-weight: 700;
            color: #222;
            margin: 10px 0;
        }

        #hero h1 {
            font-size: 56px;
            font-weight: 800;
            color: #111;
            margin: 10px 0 20px;
            line-height: 1.2;
        }

        #hero p {
        font-size: 18px;
        color: #f1f1f1; /* Light text for dark background */
        margin-bottom: 30px;
        max-width: 500px;
        text-shadow: 0 2px 4px rgba(0,0,0,0.6);
    }

    #hero h1,
    #hero h2,
    #hero h4,
    #hero p {
        color: #fff; /* all white text */
        text-shadow: 0 2px 6px rgba(0,0,0,0.4);
    }

        #hero button {
            background: linear-gradient(135deg, #28a745, #20c997);
            color: #fff;
            font-size: 18px;
            padding: 14px 36px;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 6px 20px rgba(40, 167, 69, 0.25);
        }

            #hero button:hover {
                transform: translateY(-3px);
                background: linear-gradient(135deg, #20c997, #28a745);
            }
</style>


<section id="hero">
    <h4>@Localizer["TrustOurProducts"]</h4>
    <h2>@Localizer["Supervaluedeals"]</h2>
    <h1>@Localizer["Onallproducts"]</h1>
    <p>@Localizer["Savemorewithcoupons"]</p>
    <button onclick="window.location.href='@Url.Action("Shop", "Home")'">
        @Localizer["ShopNow"]
    </button>
</section>
